# To create a conda environment for this module:
# conda env create -f environment.yml
name: nibscbioinformatics-modules-cutadapt
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - bioconda::cutadapt==2.10
